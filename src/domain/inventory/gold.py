# from random import uniform

# from domain.inventory.item import Item


# class Gold(Item):
#     def __init__(self, y: int, x: int, subtype: str, level: int):
#         super().__init__(y, x ,"Gold", subtype)
#         self.amount = int(uniform(10, 20 + level) * (level / 7))